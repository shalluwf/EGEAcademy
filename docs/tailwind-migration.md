# Статус стилей

Tailwind **удалён** из проекта: стили собираются через **PostCSS (Autoprefixer)** и единый вход `src/styles/main.css` (БЭМ-блоки + токены).

Резервная копия старой вёрстки на Tailwind: `src/App(staticx0).jsx` (не подключена к сборке).
